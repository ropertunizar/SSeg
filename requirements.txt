# SSeg dependencies — install AFTER installing torch+torchvision matching
# your CUDA driver. See INSTALL.md for the torch install step.
#
# Pinning rationale: lower bounds only. Anything tighter ages badly across
# CUDA / driver variants.

opencv-python>=4.8.0
scikit-learn>=1.3.0
numpy>=1.24,<3
matplotlib>=3.7
tqdm>=4.65
pandas>=2.0
scikit-image>=0.21
scipy>=1.10
Pillow>=10.0
hydra-core>=1.3
iopath>=0.1.10
torchmetrics>=1.0
gdown>=5.0

# Optional — only needed for app.py (interactive GUI).
# Uncomment if you want the GUI tool.
# PyQt6>=6.5
